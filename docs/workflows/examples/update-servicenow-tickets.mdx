---
title: "更新 ServiceNow 工单"
---

<Info>

链接到[工作流](https://github.com/keephq/keep/blob/main/examples/workflows/update_service_now_tickets_status.yml)。

</Info>

此示例演示了如何定期更新与告警关联的 ServiceNow 工单状态。

说明：
- 触发器：工作流可以手动触发，模拟定时执行。
- 步骤 1：使用 Keep 提供者获取所有 `ticket_type` 为 `servicenow` 的告警。
- 动作：遍历获取到的告警，并使用最新状态更新其关联的 ServiceNow 工单。


```yaml
workflow:
  id: servicenow
  description: update the ticket status every minute
  triggers:
    - type: manual
  steps:
    - name: get-alerts
      provider:
        type: keep
        with:
          cel: ticket_type == "servicenow"
  actions:
    - name: update-ticket
      foreach: "{{ steps.get-alerts.results }}"
      provider:
        type: servicenow
        config: "{{ providers.servicenow }}"
        with:
          ticket_id: "{{ foreach.value.alert_enrichment.enrichments.ticket_id }}"
          table_name: "{{ foreach.value.alert_enrichment.enrichments.table_name }}"
          fingerprint: "{{ foreach.value.alert_fingerprint }}"
          enrich_alert:
            - key: ticket_status
              value: results.state
```
