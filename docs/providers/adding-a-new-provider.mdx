---
title: "Adding a new Provider"
sidebarTitle: "Adding a New Provider"
---

本指南介绍如何为 Keep 创建新的提供者。提供者是一种集成，允许 Keep 与外部服务交互，用于告警、查询数据、管理事件或构建拓扑图。

## 目录
- [提供者结构](#provider-structure)
- [分步实现](#step-by-step-implementation)
- [提供者属性](#provider-attributes)
- [抽象方法](#abstract-methods)
- [提供者类型与能力](#provider-types-and-capabilities)
- [认证配置](#authentication-configuration)
- [测试你的提供者](#testing-your-provider)
- [最佳实践](#best-practices)
- [常见模式](#common-patterns)
- [完整提供者示例](#complete-provider-example)
- [检查清单](#checklist)

## 提供者结构

Keep 中的每个提供者遵循特定的结构：

```
keep/providers/
├── yourservice_provider/
│   ├── __init__.py
│   └── yourservice_provider.py
```

**重要说明：**
- Keep 的 ProvidersFactory 会根据目录命名约定（`*_provider`）自动发现提供者。
- 你不需要显式注册它们——只需遵循命名模式即可。
- 提供者类型会自动从类名中提取（例如，`ServiceNowProvider` → `servicenow`）。

## 分步实现

### 1. 创建提供者目录

在 `keep/providers/` 下创建一个遵循 `{service}_provider` 模式的新目录：

```bash
mkdir keep/providers/yourservice_provider
```

### 2. 创建提供者模块

创建 `yourservice_provider.py`，结构如下：

```python
"""
YourService Provider is a class that allows integration with YourService.
"""

import dataclasses
import json
import os
from typing import Optional, List, Dict, Any

import pydantic
import requests

from keep.api.models.alert import AlertDto, AlertSeverity, AlertStatus
from keep.contextmanager.contextmanager import ContextManager
from keep.providers.base.base_provider import BaseProvider
from keep.providers.models.provider_config import ProviderConfig, ProviderScope
from keep.providers.models.provider_method import ProviderMethod


@pydantic.dataclasses.dataclass
class YourserviceProviderAuthConfig:
    """YourService 认证配置。"""
    
    api_endpoint: str = dataclasses.field(
        metadata={
            "required": True,
            "description": "YourService API 端点 URL",
            "validation": "https_url",  # 可选：验证 HTTPS URL
        }
    )
    
    api_key: str = dataclasses.field(
        metadata={
            "required": True,
            "description": "YourService 的 API 密钥",
            "sensitive": True,  # 在 UI 中将字段标记为敏感信息
        }
    )
    
    region: str = dataclasses.field(
        default="us-east-1",
        metadata={
            "required": False,
            "description": "YourService 区域",
            "type": "select",
            "options": ["us-east-1", "eu-west-1", "ap-south-1"],
        }
    )


class YourserviceProvider(BaseProvider):
    """发送告警并从 YourService 获取数据。"""
    
    # 必需：UI 中显示的显示名称
    PROVIDER_DISPLAY_NAME = "YourService"
    
    # 必需：提供者分类的类别
    PROVIDER_CATEGORY = ["Monitoring"]
    
    # 可选：用于可搜索性的标签
    PROVIDER_TAGS = ["alert", "data"]
    
    # 可选：定义所需的权限范围
    PROVIDER_SCOPES = [
        ProviderScope(
            name="read:alerts",
            description="从 YourService 读取告警",
            mandatory=True,
            documentation_url="https://docs.yourservice.com/permissions",
            alias="读取告警",
        ),
        ProviderScope(
            name="write:alerts",
            description="创建和更新告警",
            mandatory=False,
            mandatory_for_webhook=True,  # 仅在 webhook 设置时需要
        ),
    ]
    
    # 可选：OAuth2 URL（必须设置为类属性，而非在 __init__ 中设置）
    OAUTH2_URL = None  # 或 os.environ.get("YOURSERVICE_OAUTH2_URL")
    
    def __init__(
        self, context_manager: ContextManager, provider_id: str, config: ProviderConfig
    ):
        super().__init__(context_manager, provider_id, config)
        # 在此处初始化任何客户端库或状态
        # 注意：Logger 会自动作为 self.logger 可用
        
        # 上下文管理器提供以下访问：
        # - self.context_manager.tenant_id: 当前租户 ID
        # - self.context_manager.workflow_id: 当前工作流 ID
        # - self.context_manager.workflow_execution_id: 当前执行 ID
        # - self.context_manager.get_full_context(): 完整工作流上下文
        
    def validate_config(self):
        """
        验证 YourService 提供者的必需配置。
        
        这是一个必须实现的抽象方法。
        """
        self.authentication_config = YourserviceProviderAuthConfig(
            **self.config.authentication
        )
        
    def dispose(self):
        """
        当提供者被销毁时清理所有资源。
        
        这是一个必须实现的抽象方法，即使它只是 pass。
        """
        pass
```

### 3. 创建 __init__.py 文件

创建 `keep/providers/yourservice_provider/__init__.py`：

```python
from keep.providers.yourservice_provider.yourservice_provider import (
    YourserviceProvider,
    YourserviceProviderAuthConfig
)

__all__ = ["YourserviceProvider", "YourserviceProviderAuthConfig"]
```

### 4. 添加提供者文档

按照文档模板创建 `docs/providers/documentation/yourservice-provider.mdx`。

<Note>
提供者配置字段通过自动生成的代码片段自动记录。Keep 根据提供者的 AuthConfig 元数据在 `docs/snippets/providers/` 中生成代码片段文件，并自动将其包含在文档中。
</Note>

## 提供者架构

### 抽象方法

每个提供者必须实现 BaseProvider 中的以下两个抽象方法：

1. **`validate_config(self)`** - 验证并处理提供者配置
2. **`dispose(self)`** - 在提供者被销毁时清理资源

### 提供者能力

提供者通过标准方法暴露能力：

- **`_notify(**kwargs)`** - 发送通知或告警
- **`_query(**kwargs)`** - 从提供者查询数据
- **`_get_alerts()`** - 获取用于监控的告警
- **`setup_webhook(...)`** - 配置 webhook 端点
- **`validate_scopes()`** - 检查提供者权限
- **`expose()`** - 返回执行期间计算的参数，供工作流使用

<Note>
公共方法 `notify()` 和 `query()` 包装了私有实现（`_notify()` 和 `_query()`），并附加了数据富化和错误处理等额外能力。始终实现私有方法。
</Note>

### 提供者发现

Keep 根据命名约定自动发现提供者：

- 位置：`keep/providers/` 目录
- 目录命名：必须以 `_provider` 结尾（例如，`slack_provider`）
- 主文件：必须与目录名称匹配，扩展名为 `.py`（例如，`slack_provider.py`）
- 无需显式注册——只需遵循命名约定即可

### 实现示例

#### Validate_config()
```python
def validate_config(self):
    """验证并处理提供者配置。"""
    self.authentication_config = YourserviceProviderAuthConfig(
        **self.config.authentication
    )
```

#### Dispose()
```python
def dispose(self):
    """清理所有资源。"""
    # 关闭连接、清理客户端等。
    # 如果无需清理，可以直接 pass
    pass
```

### 提供者类型提取

提供者类型会自动从类名中提取：
- `YourserviceProvider` → `yourservice`
- `ServiceNowProvider` → `service.now` 
- `DatadogProvider` → `datadog`

这是通过 BaseProvider 中的 `_extract_type()` 方法实现的。

### 提供者属性

提供者应定义以下类属性：

- `PROVIDER_DISPLAY_NAME`：用于 UI 显示的字符串（例如，"Slack"）
- `PROVIDER_CATEGORY`：来自允许值的类别列表（参见提供者类别部分）
- `PROVIDER_COMING_SOON`：布尔标志，将提供者标记为尚未就绪（默认值：False）
- `WEBHOOK_INSTALLATION_REQUIRED`：布尔值，使 webhook 设置在 UI 中为必填项（默认值：False）
- `PROVIDER_TAGS`：描述提供者能力的标签列表（例如，["alert", "messaging"]）
- `PROVIDER_SCOPES`：定义所需权限的 ProviderScope 对象列表
- `PROVIDER_METHODS`：用于附加能力的 ProviderMethod 对象列表（参见[提供者方法](/providers/provider-methods)）
- `FINGERPRINT_FIELDS`：用于计算告警指纹的字段名称列表
- `OAUTH2_URL`：如果提供者支持 OAuth 2.0 认证，则为 OAuth 2.0 授权 URL

### 提供者类别

提供者必须从以下列表中指定一个或多个类别：

```python
PROVIDER_CATEGORY: list[Literal[
    "AI", "Monitoring", "Incident Management", "Cloud Infrastructure",
    "Ticketing", "Identity", "Developer Tools", "Database",
    "Identity and Access Management", "Security", "Collaboration",
    "Organizational Tools", "CRM", "Queues", "Orchestration", "Others"
]]
```

### 提供者标签

`PROVIDER_TAGS` 的有效选项：
- `"alert"` - 提供者处理告警
- `"ticketing"` - 提供者管理工单
- `"messaging"` - 提供者发送消息
- `"data"` - 提供者查询数据
- `"queue"` - 提供者管理队列
- `"topology"` - 提供者提供拓扑数据
- `"incident"` - 提供者管理事件

### 提供者权限范围

```python
@dataclass
class ProviderScope:
    """
    提供者权限范围模型。

    Args:
        name (str): 权限范围的名称。
        description (Optional[str]): 权限范围的描述。
        mandatory (bool): 权限范围是否为必填项。
        mandatory_for_webhook (bool): 权限范围是否对于 webhook 自动安装为必填项。
        documentation_url (Optional[str]): 权限范围的文档 URL。
        alias (Optional[str]): 权限范围的别名。
    """

    name: str
    description: Optional[str] = None
    mandatory: bool = False
    mandatory_for_webhook: bool = False
    documentation_url: Optional[str] = None
    alias: Optional[str] = None
```

### 提供者配置

```python
@dataclass
class ProviderConfig:
    """
    提供者配置模型。

    Args:
        description (Optional[str]): 提供者的描述。
        authentication (dict): 提供者的配置。
    """

    authentication: Optional[dict]
    name: Optional[str] = None
    description: Optional[str] = None

    def __post_init__(self):
        if not self.authentication:
            return
        for key, value in self.authentication.items():
            if (
                isinstance(value, str)
                and value.startswith("{{")
                and value.endswith("}}")
            ):
                self.authentication[key] = chevron.render(value, {"env": os.environ})
```

### 基础提供者

```python
"""
所有提供者的基类。
"""
class BaseProvider(metaclass=abc.ABCMeta):
    OAUTH2_URL = None
    PROVIDER_SCOPES: list[ProviderScope] = []
    PROVIDER_METHODS: list[ProviderMethod] = []
    FINGERPRINT_FIELDS: list[str] = []
    PROVIDER_TAGS: list[
        Literal["alert", "ticketing", "messaging", "data", "queue", "topology", "incident"]
    ] = []
    PROVIDER_DISPLAY_NAME: str = None
    PROVIDER_CATEGORY: list[str] = []
    PROVIDER_COMING_SOON: bool = False
    WEBHOOK_INSTALLATION_REQUIRED: bool = False

    def __init__(
        self,
        context_manager: ContextManager,
        provider_id: str,
        config: ProviderConfig,
        webhook_template: Optional[str] = None,
        webhook_description: Optional[str] = None,
        webhook_markdown: Optional[str] = None,
        provider_description: Optional[str] = None,
    ):
        """
        初始化一个提供者。

        Args:
            provider_id (str): 提供者 ID。
            **kwargs: 从提供者 YAML 文件加载的提供者配置。
        """
        self.provider_id = provider_id

        self.config = config
        self.webhook_template = webhook_template
        self.webhook_description = webhook_description
        self.provider_description = provider_description
        self.context_manager = context_manager
        self.logger = context_manager.get_logger()
        self.validate_config()
        self.logger.debug(
            "Base provider initalized", extra={"provider": self.__class__.__name__}
        )
        self.provider_type = self._extract_type()
        self.results = []
        # tb: 我们可以在初始化提供者时通过客户配置覆盖此项
        self.fingerprint_fields = self.FINGERPRINT_FIELDS

    def _extract_type(self):
        """
        从提供者类名中提取提供者类型。

        Returns:
            str: 提供者类型。
        """
        name = self.__class__.__name__
        name_without_provider = name.replace("Provider", "")
        name_with_spaces = (
            re.sub("([A-Z])", r" \1", name_without_provider).lower().strip()
        )
        return name_with_spaces.replace(" ", ".")

    @abc.abstractmethod
    def dispose(self):
        """
        销毁提供者。
        """
        raise NotImplementedError("dispose() method not implemented")

    @abc.abstractmethod
    def validate_config(self):
        """
        验证提供者配置。
        """
        raise NotImplementedError("validate_config() method not implemented")

    def validate_scopes(self) -> dict[str, bool | str]:
        """
        验证提供者权限范围。

        Returns:
            dict: 其中键是权限范围名称，值是权限范围是否有效（True 布尔值）或带有错误消息的字符串。
        """
        return {}

    def notify(self, **kwargs):
        """
        输出告警消息。

        Args:
            **kwargs (dict): 提供者上下文（with 语句）
        """
        # 触发提供者
        results = self._notify(**kwargs)
        self.results.append(results)
        # 如果告警需要富化，则进行富化
        enrich_alert = kwargs.get("enrich_alert", [])
        if not enrich_alert or not results:
            return results if results else None

        self._enrich(enrich_alert, results)
        return results

    def _enrich(self, enrichments, results, audit_enabled=True):
        """
        用提供者特定数据富化告警或事件。
        
        此方法替代了已弃用的 _enrich_alert 方法，同时支持告警和事件富化。
        
        Args:
            enrichments: 富化配置列表
            results: 提供者操作的结果
            audit_enabled: 是否审计富化操作（默认值：True）
        """
        self.logger.debug("Extracting the fingerprint from the alert")
        if "fingerprint" in results:
            fingerprint = results["fingerprint"]
        elif self.context_manager.foreach_context.get("value", {}):
            # TODO: 如果是压缩的，我们需要从压缩中提取指纹（即多个 foreach）
            fingerprint = self.context_manager.foreach_context.get("value", {}).get(
                "fingerprint"
            )
        # 否则，如果我们在事件上下文中，使用事件指纹
        elif self.context_manager.event_context:
            # TODO: 将所有 event_context 是 dict 的情况映射到 DTO
            #       然后移除此 if 语句
            if isinstance(self.context_manager.event_context, dict):
                fingerprint = self.context_manager.event_context.get("fingerprint")
            # Alert DTO
            else:
                fingerprint = self.context_manager.event_context.fingerprint
        else:
            fingerprint = None

        if not fingerprint:
            self.logger.error(
                "No fingerprint found for alert enrichment",
                extra={"provider": self.provider_id},
            )
            raise Exception("No fingerprint found for alert enrichment")
        self.logger.debug("Fingerprint extracted", extra={"fingerprint": fingerprint})

        _enrichments = {}
        # 仅富化请求的字段
        for enrichment in enrichments:
            try:
                if enrichment["value"].startswith("results."):
                    val = enrichment["value"].replace("results.", "")
                    parts = val.split(".")
                    r = copy.copy(results)
                    for part in parts:
                        r = r[part]
                    _enrichments[enrichment["key"]] = r
                else:
                    _enrichments[enrichment["key"]] = enrichment["value"]
            except Exception:
                self.logger.error(
                    f"Failed to enrich alert - enrichment: {enrichment}",
                    extra={"fingerprint": fingerprint, "provider": self.provider_id},
                )
                continue
        self.logger.info("Enriching alert", extra={"fingerprint": fingerprint})
        try:
            enrich_alert(self.context_manager.tenant_id, fingerprint, _enrichments)
        except Exception as e:
            self.logger.error(
                "Failed to enrich alert in db",
                extra={"fingerprint": fingerprint, "provider": self.provider_id},
            )
            raise e
        self.logger.info("Alert enriched", extra={"fingerprint": fingerprint})

    def _notify(self, **kwargs):
        """
        输出告警消息。

        Args:
            **kwargs (dict): 提供者上下文（with 语句）
        """
        raise NotImplementedError("notify() method not implemented")

    def _query(self, **kwargs: dict):
        """
        使用给定的查询对提供者进行查询。

        Args:
            kwargs (dict): 提供者上下文（with 语句）

        Raises:
            NotImplementedError: _description_
        """
        raise NotImplementedError("query() method not implemented")

    def query(self, **kwargs: dict):
        # 直接运行查询
        results = self._query(**kwargs)
        # 现在将结果类型添加到全局上下文
        if results and isinstance(results, list):
            self.context_manager.dependencies.add(results[0].__class__)
        elif results:
            self.context_manager.dependencies.add(results.__class__)

        enrich_alert = kwargs.get("enrich_alert", [])
        if enrich_alert:
            self._enrich(enrich_alert, results)
        # 并返回结果
        return results

    @staticmethod
    def _format_alert(
        event: dict | list[dict], provider_instance: "BaseProvider" = None
    ) -> AlertDto | list[AlertDto]:
        """
        将传入事件格式化为 AlertDto 对象。
        
        Args:
            event: 单个事件 dict 或事件 dict 列表
            provider_instance: 可选的提供者实例，用于上下文
            
        Returns:
            AlertDto 或 AlertDto 对象列表
        """
        raise NotImplementedError("format_alert() method not implemented")

    @classmethod
    def format_alert(cls, event: dict) -> AlertDto | list[AlertDto]:
        logger = logging.getLogger(__name__)
        logger.debug("Formatting alert")
        formatted_alert = cls._format_alert(event)
        logger.debug("Alert formatted")
        return formatted_alert

    @staticmethod
    def get_alert_fingerprint(alert: AlertDto, fingerprint_fields: list = []) -> str:
        """
        获取告警的指纹。

        Args:
            event (AlertDto): 要获取指纹的告警。
            fingerprint_fields (list, optional): 我们计算指纹所依据的字段。默认为 []。

        Returns:
            str: 指纹的 hexdigest 或 event.name（如果没有给出 fingerprint_fields）。
        """
        if not fingerprint_fields:
            return alert.name
        fingerprint = hashlib.sha256()
        event_dict = alert.dict()
        for fingerprint_field in fingerprint_fields:
            fingerprint_field_value = event_dict.get(fingerprint_field, None)
            if isinstance(fingerprint_field_value, (list, dict)):
                fingerprint_field_value = json.dumps(fingerprint_field_value)
            if fingerprint_field_value:
                fingerprint.update(str(fingerprint_field_value).encode())
        return fingerprint.hexdigest()

    def get_alerts_configuration(self, alert_id: Optional[str] = None):
        """
        从提供者获取告警配置。

        Args:
            alert_id (Optional[str], optional): 如果提供，则按 ID 获取特定告警。默认为 None。
        """
        # todo: 我们希望为所有提供者建立一个通用告警模型（也为了 GPT 输出的一致性）
        raise NotImplementedError("get_alerts() method not implemented")

    def deploy_alert(self, alert: dict, alert_id: Optional[str] = None):
        """
        将告警部署到提供者。

        Args:
            alert (dict): 要部署的告警。
            alert_id (Optional[str], optional): 如果提供，则按 ID 部署特定告警。默认为 None。
        """
        raise NotImplementedError("deploy_alert() method not implemented")

    def _get_alerts(self) -> list[AlertDto]:
        """
        从提供者获取告警。
        """
        raise NotImplementedError("get_alerts() method not implemented")

    def get_alerts(self) -> list[AlertDto]:
        """
        从提供者获取告警。
        """
        with tracer.start_as_current_span(f"{self.__class__.__name__}-get_alerts"):
            alerts = self._get_alerts()
            # 用提供者 ID 富化告警
            for alert in alerts:
                alert.providerId = self.provider_id
            return alerts

    def get_alerts_by_fingerprint(self, tenant_id: str) -> dict[str, list[AlertDto]]:
        """
        从提供者获取按指纹分组的告警，按 lastReceived 排序。

        Returns:
            dict[str, list[AlertDto]]: 按指纹分组的告警字典，按 lastReceived 排序。
        """
        alerts = self.get_alerts()

        if not alerts:
            return {}

        # 获取告警，按指纹分组并按 lastReceived 排序
        with tracer.start_as_current_span(f"{self.__class__.__name__}-get_last_alerts"):
            get_attr = operator.attrgetter("fingerprint")
            grouped_alerts = {
                fingerprint: list(alerts)
                for fingerprint, alerts in itertools.groupby(
                    sorted(
                        alerts,
                        key=get_attr,
                    ),
                    get_attr,
                )
            }

        # 富化告警
        with tracer.start_as_current_span(f"{self.__class__.__name__}-enrich_alerts"):
            pulled_alerts_enrichments = get_enrichments(
                tenant_id=tenant_id,
                fingerprints=grouped_alerts.keys(),
            )
            for alert_enrichment in pulled_alerts_enrichments:
                if alert_enrichment:
                    alerts_to_enrich = grouped_alerts.get(
                        alert_enrichment.alert_fingerprint
                    )
                    for alert_to_enrich in alerts_to_enrich:
                        parse_and_enrich_deleted_and_assignees(
                            alert_to_enrich, alert_enrichment.enrichments
                        )
                        for enrichment in alert_enrichment.enrichments:
                            # 设置富化内容
                            setattr(
                                alert_to_enrich,
                                enrichment,
                                alert_enrichment.enrichments[enrichment],
                            )

        return grouped_alerts

    def setup_webhook(
        self, tenant_id: str, keep_api_url: str, api_key: str, setup_alerts: bool = True
    ) -> dict | None:
        """
        为提供者设置 webhook。

        Args:
            tenant_id (str): 租户 ID
            keep_api_url (str): 用于 webhook 回调的 Keep API URL
            api_key (str): 用于认证的 API 密钥
            setup_alerts (bool, optional): 是否设置告警。默认为 True。

        Returns:
            dict | None: 如果需要保存的密钥字典，否则为 None
            
        Raises:
            NotImplementedError: 如果提供者未实现
        """
        raise NotImplementedError("setup_webhook() method not implemented")

    @staticmethod
    def get_alert_schema() -> dict:
        """
        获取提供者的告警模式描述。
            例如，如何为提供者定义可以通过 API 推送的告警。

        Returns:
            str: 告警格式描述。
        """
        raise NotImplementedError(
            "get_alert_format_description() method not implemented"
        )

    @staticmethod
    def oauth2_logic(**payload) -> dict:
        """
        OAuth2 认证逻辑。

        例如，在 Slack OAuth2 中，我们需要从 payload 中获取 code 并将其交换为 token。

        return: dict: 要保存为提供者配置的密钥。（例如 Slack 访问令牌）
        """
        raise NotImplementedError("oauth2_logic() method not implemented")

    @staticmethod
    def parse_event_raw_body(raw_body: bytes | dict) -> dict:
        """
        解析事件的原始 body 并从中创建可摄入的 dict。

        例如，在 Parseable 中，"event" 只是一个字符串
        > b'Alert: Server side error triggered on teststream1\nMessage: server reporting status as 500\nFailing Condition: status column equal to abcd, 2 times'
        我们想返回一个对象
        > {'alert': 'Server side error triggered on teststream1', 'message': 'server reporting status as 500', 'failing_condition': 'status column equal to abcd, 2 times'}

        如果提供者未实现此方法，它应该将原始 body 转换为 dict。

        Args:
            raw_body (bytes | dict): 传入事件的原始 body（可以是 bytes 或 dict）

        Returns:
            dict: 可摄入的事件字典
        """
        if isinstance(raw_body, dict):
            return raw_body
        return raw_body

    def get_logs(self, limit: int = 5) -> list:
        """
        从提供者获取日志。

        Args:
            limit (int): 要获取的日志数量。
        """
        raise NotImplementedError("get_logs() method not implemented")

    def expose(self):
        """暴露在查询期间计算的参数。

        每个提供者可以暴露在查询期间计算的参数。
        例如，由用户提供并由提供者渲染的参数。

        一个具体例子是 Datadog 提供者的 "_from" 和 "to"，它们在执行期间计算。
        """
        # TODO - 使用装饰器动态实现
        return {}

    def start_consume(self):
        """获取提供者的消费者。

        如果提供者有消费者，则应由提供者实现。

        有关示例，请参阅 Kafka 提供者

        Returns:
            Consumer: 提供者的消费者。
        """
        return

    def status(self) -> bool:
        """返回提供者的状态。

        Returns:
            bool: 提供者的状态。
        """
        return {
            "status": "如果提供者有消费者，则应实现此方法",
            "error": "",
        }

    @property
    def is_consumer(self) -> bool:
        """如果继承的类有 start_consume 方法，则返回 consumer。

        Returns:
            bool: _description_
        """
        return self.start_consume.__qualname__ != "BaseProvider.start_consume"

    def _push_alert(self, alert: dict):
        """
        将告警推送到提供者。

        Args:
            alert (dict): 要推送的告警。
        """
        # 如果这不是 dict，尝试将其转换为 dict
        if not isinstance(alert, dict):
            try:
                alert_data = json.loads(alert)
            except Exception:
                alert_data = alert_data
        else:
            alert_data = alert

        # 如果这仍然不是 dict，我们无法推送它
        if not isinstance(alert_data, dict):
            self.logger.warning(
                "We currently support only alert represented as a dict, dismissing alert",
                extra={"alert": alert},
            )
            return
        # 现在尝试构建告警模型
        # 我们将有很多默认值来支持所有提供者和所有情况，
        # 微调的方法是使用提供者特定模型或强制将队列中的事件转换为字段
        alert_model = AlertDto(
            id=alert_data.get("id", str(uuid.uuid4())),
            name=alert_data.get("name", "alert-from-event-queue"),
            status=alert_data.get("status", AlertStatus.FIRING),
            lastReceived=alert_data.get("lastReceived", datetime.datetime.now()),
            environment=alert_data.get("environment", "alert-from-event-queue"),
            isDuplicate=alert_data.get("isDuplicate", False),
            duplicateReason=alert_data.get("duplicateReason", None),
            service=alert_data.get("service", "alert-from-event-queue"),
            source=alert_data.get("source", [self.provider_type]),
            message=alert_data.get("message", "alert-from-event-queue"),
            description=alert_data.get("description", "alert-from-event-queue"),
            severity=alert_data.get("severity", AlertSeverity.INFO),
            pushed=alert_data.get("pushed", False),
            event_id=alert_data.get("event_id", str(uuid.uuid4())),
            url=alert_data.get("url", None),
            fingerprint=alert_data.get("fingerprint", None),
        )
        # 将告警推送到提供者
        url = f'{os.environ["KEEP_API_URL"]}/alerts/event'
        headers = {
            "Content-Type": "application/json",
            "Accept": "application/json",
            "X-API-KEY": self.context_manager.api_key,
        }
        response = requests.post(url, json=alert_model.dict(), headers=headers)
        try:
            response.raise_for_status()
            self.logger.info("Alert pushed successfully")
        except Exception:
            self.logger.error(
                f"Failed to push alert to {self.provider_id}: {response.content}"
            )
```

## 提供者类型与能力

### 基础提供者类型

Keep 支持几种基础提供者类型，每种都有特定的能力：

1. **BaseProvider** (`keep/providers/base/base_provider.py`)
   - 基本提供者能力
   - 方法：`_notify()`、`_query()`、`_get_alerts()`
   - 适用于：通用集成

2. **BaseTopologyProvider** (`keep/providers/base/base_provider.py`)
   - 扩展 BaseProvider
   - 方法：`pull_topology()` 
   - 适用于：提供基础设施拓扑数据的服务
   - 示例：Datadog 提供者 (`keep/providers/datadog_provider/datadog_provider.py`)

3. **BaseIncidentProvider** (`keep/providers/base/base_provider.py`)
   - 扩展 BaseProvider
   - 方法：`_get_incidents()`、`_format_incident()`（静态）、`format_incident()`（类方法）、`setup_incident_webhook()`
   - 适用于：事件管理系统
   - 示例：PagerDuty 提供者 (`keep/providers/pagerduty_provider/pagerduty_provider.py`)

### 通用能力

#### 1. 通知 (`_notify`)
向外部服务发送告警或消息：
```python
def _notify(self, title: str, description: str = "", **kwargs) -> dict:
    # 实现
```

#### 2. 查询 (`_query`)
从外部服务获取数据：
```python
def _query(self, query: str, **kwargs) -> list:
    # 实现
```

#### 3. 告警拉取 (`_get_alerts`)
拉取告警用于监控：
```python
def _get_alerts(self) -> List[AlertDto]:
    # 实现
```

#### 4. Webhook 支持
处理传入的 webhook：
```python
@staticmethod
def parse_event_raw_body(raw_body: bytes | str) -> dict:
    # 解析 webhook 负载
    
@staticmethod
def _format_alert(event: dict, provider_instance: "BaseProvider" = None) -> AlertDto | list[AlertDto]:
    # 将 webhook 事件格式化为告警
```

#### 5. OAuth 2.0 支持
处理 OAuth 2.0 认证：
```python
# 重要：将 OAUTH2_URL 定义为类级别的类属性，而不是在 __init__ 中
class YourserviceProvider(BaseProvider):
    OAUTH2_URL = os.environ.get("YOURSERVICE_OAUTH2_URL")  # 必须在类级别

@staticmethod
def oauth2_logic(**payload) -> dict:
    # OAuth 2.0 实现
```

#### 6. 消费者提供者
用于从队列或流中消费消息的提供者：
```python
def start_consume(self):
    """
    开始从提供者消费消息。
    
    当 Keep 将提供者作为消费者启动时，会调用此方法。
    在此处实现长时间运行的消费逻辑。
    """
    # 示例：Kafka 消费者
    while True:
        message = self.consumer.poll()
        if message:
            self._push_alert(message)
            
@property
def is_consumer(self) -> bool:
    """如果实现了 start_consume，提供者会自动被检测为消费者。"""
    return True  # 如果覆盖了 start_consume，则自动设置
    
def status(self) -> dict:
    """返回消费者的状态。"""
    return {
        "status": "running" if self.consumer_active else "stopped",
        "error": self.last_error if hasattr(self, 'last_error') else ""
    }
```

### 专用基类

Keep 为特定提供者类型提供了专用基类：

#### BaseTopologyProvider

用于管理基础设施拓扑和服务依赖的提供者：

```python
from keep.providers.base.base_topology_provider import BaseTopologyProvider

class MyTopologyProvider(BaseTopologyProvider):
    def pull_topology(self) -> tuple[list[TopologyServiceInDto], dict]:
        """
        从提供者拉取拓扑数据。
        
        Returns:
            tuple: 一个包含（服务列表，边字典）的元组
        """
        # 实现拓扑拉取逻辑
        pass
```

#### BaseIncidentProvider

用于管理事件和事件响应的提供者：

```python
from keep.providers.base.base_incident_provider import BaseIncidentProvider

class MyIncidentProvider(BaseIncidentProvider):
    def _get_incidents(self) -> list[IncidentDto]:
        """
        从提供者获取事件（抽象方法）。
        
        Returns:
            list[IncidentDto]: 事件列表
        """
        # 实现事件拉取逻辑
        pass
    
    @staticmethod
    def _format_incident(
        event: dict, provider_instance: "BaseProvider" = None
    ) -> IncidentDto | list[IncidentDto]:
        """
        将原始事件数据格式化为 IncidentDto 对象。
        
        Args:
            event: 来自 webhook 或 API 的原始事件数据
            provider_instance: 可选的提供者实例，用于上下文
            
        Returns:
            IncidentDto 或 IncidentDto 对象列表
        """
        # 实现事件格式化逻辑
        pass
    
    def setup_incident_webhook(
        self,
        tenant_id: str,
        keep_api_url: str,
        api_key: str,
        setup_alerts: bool = True,
    ) -> dict | None:
        """
        设置用于事件更新的 webhook。
        
        Args:
            tenant_id: 租户标识符
            keep_api_url: 用于回调的 Keep API URL
            api_key: 用于认证的 API 密钥
            setup_alerts: 是否同时设置告警 webhook
            
        Returns:
            dict | None: 如果有需要保存的密钥
        """
        # 实现 webhook 设置逻辑
        pass
```

注意：`get_incidents()` 方法由基类自动提供，并包装 `_get_incidents()`。`format_incident()` 类方法处理提供者加载并调用 `_format_incident()`。

### 认证配置

提供者应将认证配置类定义为具有正确字段类型和验证的 dataclass：

```python
import dataclasses
import pydantic
from keep.validation.fields import HttpsUrl, NoSchemeUrl, UrlPort

@pydantic.dataclasses.dataclass
class MyProviderAuthConfig:
    """MyProvider 认证的配置。"""
    
    api_key: str = dataclasses.field(
        metadata={
            "required": True,
            "description": "用于认证的 API 密钥",
            "sensitive": True,  # 在 UI 中掩码字段值
        }
    )
    
    api_url: HttpsUrl = dataclasses.field(
        default="https://api.example.com",
        metadata={
            "required": False,
            "description": "API 端点 URL（仅 HTTPS）",
            "documentation_url": "https://docs.example.com/api",
            "validation": "https_url",  # 映射到 HttpsUrl 验证器
        }
    )
    
    host: NoSchemeUrl = dataclasses.field(
        metadata={
            "required": True,
            "description": "服务主机名",
            "hint": "example.com 或 192.168.1.1",
            "validation": "no_scheme_url",  # 映射到 NoSchemeUrl 验证器
        }
    )
    
    port: UrlPort = dataclasses.field(
        default=443,
        metadata={
            "required": False,
            "description": "服务端口",
            "validation": "port",  # 验证端口范围 1-65535
        }
    )
    
    workspace_id: str = dataclasses.field(
        metadata={
            "required": True,
            "description": "工作区标识符",
            "hint": "可在 设置 > 工作区 中找到",
        }
    )
    
    region: str = dataclasses.field(
        default="us-east-1",
        metadata={
            "required": False,
            "description": "服务区域",
            "type": "select",  # 在 UI 中渲染为下拉菜单
            "options": ["us-east-1", "eu-west-1", "ap-south-1"],
        }
    )
```

#### 字段验证

Keep 通过自定义 Pydantic 字段类型提供内置字段验证：

| 验证类型 | 字段类型 | 描述 | 示例 |
|----------------|------------|-------------|---------|
| `"https_url"` | `HttpsUrl` | 仅验证 HTTPS URL | `https://api.example.com` |
| `"any_http_url"` | `pydantic.AnyHttpUrl` | 验证任何 HTTP/HTTPS URL | `http://example.com` |
| `"no_scheme_url"` | `NoSchemeUrl` | 验证无 scheme 的 URL | `example.com:8080` |
| `"port"` | `UrlPort` | 验证端口号（1-65535） | `443` |
| `"multihost_url"` | `MultiHostUrl` | 验证多主机 URL | `mongodb://host1:27017,host2:27017` |
| `"no_scheme_multihost_url"` | `NoSchemeMultiHostUrl` | 无 scheme 的多主机 URL | `host1:9092,host2:9092` |

使用验证的方法：
1. 从 `keep.validation.fields` 导入相应的字段类型
2. 将其用作字段类型注解
3. 在元数据中添加相应的验证字符串

实现示例：

```python
# 仅 HTTPS 的 webhook URL
webhook_url: HttpsUrl = dataclasses.field(
    metadata={
        "required": True,
        "description": "Webhook 端点（需要 HTTPS）",
        "sensitive": True,
        "validation": "https_url",
    }
)

# 具有多个主机的数据库连接
connection_string: MultiHostUrl = dataclasses.field(
    metadata={
        "required": True,
        "description": "数据库连接字符串",
        "hint": "mongodb://host1:27017,host2:27017/dbname",
        "validation": "multihost_url",
    }
)

# SSH 连接
ssh_host: NoSchemeUrl = dataclasses.field(
    metadata={
        "required": True,
        "description": "SSH 主机名或 IP",
        "validation": "no_scheme_url",
    }
)

ssh_port: UrlPort = dataclasses.field(
    default=22,
    metadata={
        "required": False,
        "description": "SSH 端口",
        "validation": "port",
    }
)
```

#### 元数据字段参考

- `required`：字段是否为必填项
- `description`：在 UI 中显示的字段描述
- `sensitive`：是否掩码字段值（用于密钥）
- `hidden`：是否在 UI 中隐藏字段
- `documentation_url`：相关文档的链接
- `hint`：用户的帮助文本
- `validation`：验证类型字符串（参见前面的表格）
- `type`：UI 输入类型（例如，"select" 表示下拉菜单）
- `options`：select 字段的有效选项列表
- `config_main_group`：在 UI 中组织字段的组名
- `config_sub_group`：嵌套组织的子组名

<Note>
验证系统确保在 Keep 实例化提供者之前配置值是有效的。无效值会被拒绝并显示清晰的错误消息，从而改善用户体验并防止运行时错误。
</Note>

## 测试你的提供者

### 1. 单元测试

创建 `tests/test_yourservice_provider.py`：

```python
import pytest
from keep.providers.yourservice_provider.yourservice_provider import YourserviceProvider
from keep.providers.models.provider_config import ProviderConfig
from keep.contextmanager.contextmanager import ContextManager


def test_yourservice_provider_init():
    """测试提供者初始化。"""
    config = ProviderConfig(
        authentication={
            "api_endpoint": "https://api.yourservice.com",
            "api_key": "test-key",
        }
    )
    
    context_manager = ContextManager(tenant_id="test", workflow_id="test")
    provider = YourserviceProvider(
        context_manager=context_manager,
        provider_id="test",
        config=config
    )
    
    assert provider.authentication_config.api_endpoint == "https://api.yourservice.com"
    assert provider.authentication_config.api_key == "test-key"


@pytest.fixture
def mock_requests(monkeypatch):
    """Mock requests 模块。"""
    import requests
    class MockResponse:
        def __init__(self, json_data, status_code=200):
            self.json_data = json_data
            self.status_code = status_code
        
        def json(self):
            return self.json_data
        
        def raise_for_status(self):
            pass
    
    def mock_post(*args, **kwargs):
        return MockResponse({"success": True})
    
    def mock_get(*args, **kwargs):
        return MockResponse({"alerts": []})
    
    monkeypatch.setattr(requests, "post", mock_post)
    monkeypatch.setattr(requests, "get", mock_get)


def test_yourservice_notify(mock_requests):
    """测试通知发送。"""
    config = ProviderConfig(
        authentication={
            "api_endpoint": "https://api.yourservice.com",
            "api_key": "test-key",
        }
    )
    
    context_manager = ContextManager(tenant_id="test", workflow_id="test")
    provider = YourserviceProvider(
        context_manager=context_manager,
        provider_id="test",
        config=config
    )
    
    result = provider.notify(message="Test message")
    assert result["success"] is True
```

### 2. 集成测试

使用提供者工厂进行测试：

```python
def test_provider_factory_loading():
    """测试提供者是否通过工厂正确加载。"""
    from keep.providers.providers_factory import ProvidersFactory
    
    # 获取提供者类
    provider_class = ProvidersFactory.get_provider_class("yourservice")
    assert provider_class.__name__ == "YourserviceProvider"
    
    # 获取所有提供者
    all_providers = ProvidersFactory.get_all_providers()
    yourservice = next((p for p in all_providers if p.type == "yourservice"), None)
    assert yourservice is not None
    assert yourservice.display_name == "YourService"
```

### 3. 手动测试

你可以通过直接运行提供者来测试它：
```bash
cd keep
python -m keep.providers.yourservice_provider.yourservice_provider
```

`if __name__ == "__main__":` 代码块允许你测试提供者初始化和基本能力。

在你的提供者中添加一个测试代码块，用于直接执行：

```python
if __name__ == "__main__":
    # 直接测试提供者
    import logging
    
    logging.basicConfig(level=logging.DEBUG, handlers=[logging.StreamHandler()])
    context_manager = ContextManager(
        tenant_id="singletenant",
        workflow_id="test",
    )
    
    # 使用测试配置初始化提供者
    config = ProviderConfig(
        authentication={
            "api_endpoint": "https://api.yourservice.com",
            "api_key": "test-key",
        }
    )
    
    provider = YourserviceProvider(
        context_manager=context_manager,
        provider_id="test",
        config=config
    )
    
    # 测试提供者方法
    print("Provider initialized successfully!")
    
    # 测试特定功能
    try:
        result = provider._query("test query")
        print(f"Query result: {result}")
    except Exception as e:
        print(f"Query failed: {e}")
```

## 最佳实践

### 1. 错误处理

始终优雅地处理 API 错误：

```python
from keep.exceptions.provider_exception import ProviderException

try:
    response = requests.get(url)
    response.raise_for_status()
except requests.exceptions.RequestException as e:
    raise ProviderException(f"Failed to fetch data: {str(e)}")
```

### 2. 日志记录

使用提供者的 logger：

```python
self.logger.info("Fetching alerts from YourService")
self.logger.error(f"Failed to connect: {str(e)}")
```

### 3. 配置验证

在 `validate_config()` 中验证配置：

```python
def validate_config(self):
    self.authentication_config = YourserviceProviderAuthConfig(
        **self.config.authentication
    )
    
    # 附加验证
    if not self.authentication_config.api_endpoint.startswith("https://"):
        raise ValueError("API endpoint must use HTTPS")
```

### 4. 告警格式化

返回告警时，使用 Keep 的标准格式：

```python
from keep.api.models.alert import AlertDto, AlertSeverity, AlertStatus

alert = AlertDto(
    id="unique-alert-id",
    name="Alert Title",
    description="Detailed description",
    severity=AlertSeverity.HIGH,
    status=AlertStatus.FIRING,
    lastReceived=datetime.now().isoformat(),
    source=["yourservice"],
    fingerprint="unique-fingerprint",
    labels={"key": "value"},
    annotations={"runbook": "https://docs.example.com"},
)
```

### 5. 密钥管理

永远不要硬编码密钥。使用环境变量或配置：

```python
client_id = os.environ.get("YOURSERVICE_CLIENT_ID")
if not client_id:
    raise ProviderException("YOURSERVICE_CLIENT_ID environment variable not set")
```

## 常见模式

### 1. 提供者健康检查

使用 `ProviderHealthMixin` 实现健康监控：

```python
from keep.providers.base.base_provider import BaseProvider, ProviderHealthMixin

class YourserviceProvider(BaseProvider, ProviderHealthMixin):
    HAS_HEALTH_CHECK = True
    
    # mixin 提供以下自动健康检查：
    # - 拓扑覆盖率验证
    # - 垃圾告警检测
    # - 告警规则使用监控
```

<Note>
健康检查 mixin 对于监控收集拓扑数据或处理大量告警的提供者特别有用。
</Note>

### 2. 分页

处理分页的 API 响应：

```python
def _get_all_items(self):
    items = []
    page = 1
    
    while True:
        response = self._query_page(page)
        items.extend(response["items"])
        
        if not response.get("has_next"):
            break
        page += 1
    
    return items
```

### 3. 速率限制

遵守 API 速率限制：

```python
import time
from typing import Any

def _rate_limited_request(self, url: str, **kwargs) -> Any:
    max_retries = 3
    
    for attempt in range(max_retries):
        try:
            response = requests.get(url, **kwargs)
            if response.status_code == 429:  # 被限速
                retry_after = int(response.headers.get("Retry-After", 60))
                self.logger.warning(f"Rate limited, waiting {retry_after}s")
                time.sleep(retry_after)
                continue
            response.raise_for_status()
            return response.json()
        except Exception as e:
            if attempt == max_retries - 1:
                raise
            time.sleep(2 ** attempt)  # 指数退避
```

### 4. 缓存

缓存频繁访问的数据：

```python
from datetime import datetime, timedelta

class YourserviceProvider(BaseProvider):
    def __init__(self, context_manager, provider_id, config):
        super().__init__(context_manager, provider_id, config)
        self._cache = {}
        self._cache_ttl = timedelta(minutes=5)
    
    def _get_cached_data(self, key: str) -> Any:
        if key in self._cache:
            data, timestamp = self._cache[key]
            if datetime.now() - timestamp < self._cache_ttl:
                return data
        return None
    
    def _set_cached_data(self, key: str, data: Any):
        self._cache[key] = (data, datetime.now())
```

### 5. Webhook 签名验证

验证 webhook 的真实性：

```python
import hmac
import hashlib

@staticmethod
def verify_webhook_signature(raw_body: bytes, signature: str, secret: str) -> bool:
    expected = hmac.new(
        secret.encode(),
        raw_body,
        hashlib.sha256
    ).hexdigest()
    return hmac.compare_digest(expected, signature)
```

### 6. 暴露运行时参数

使用 `expose()` 方法使运行时计算的值可用于工作流：

```python
class YourserviceProvider(BaseProvider):
    def __init__(self, context_manager, provider_id, config):
        super().__init__(context_manager, provider_id, config)
        self._from_timestamp = None
        self._to_timestamp = None
    
    def _query(self, metric: str, from_time: str = "1h", **kwargs):
        # 计算实际时间戳
        self._to_timestamp = datetime.now()
        self._from_timestamp = self._to_timestamp - parse_duration(from_time)
        
        # 使用计算的时间戳进行查询
        return self._fetch_metrics(metric, self._from_timestamp, self._to_timestamp)
    
    def expose(self):
        """暴露计算的参数供工作流使用。"""
        exposed = {}
        if self._from_timestamp:
            exposed["from"] = self._from_timestamp.isoformat()
        if self._to_timestamp:
            exposed["to"] = self._to_timestamp.isoformat()
        return exposed
```

这允许工作流访问查询中使用的实际时间戳，而不仅仅是相对时间字符串。

## 完整提供者示例

以下是一个完整提供者实现的最小示例：

```python
from keep.providers.base.base_provider import BaseProvider
from keep.providers.models.provider_config import ProviderConfig
from keep.contextmanager.contextmanager import ContextManager

class MyProvider(BaseProvider):
    PROVIDER_DISPLAY_NAME = "My Service"
    PROVIDER_CATEGORY = ["Monitoring", "Incident Management"]
    PROVIDER_TAGS = ["alert", "messaging"]
    
    def __init__(
        self,
        context_manager: ContextManager,
        provider_id: str,
        config: ProviderConfig,
        webhook_template: Optional[str] = None,
        webhook_description: Optional[str] = None,
        webhook_markdown: Optional[str] = None,
        provider_description: Optional[str] = None,
    ):
        super().__init__(
            context_manager, provider_id, config, 
            webhook_template, webhook_description,
            webhook_markdown, provider_description
        )
        
    def validate_config(self):
        # 验证提供者配置
        pass
        
    def dispose(self):
        # 清理资源
        pass
        
    def _query(self, **kwargs):
        # 实现查询逻辑
        pass
        
    def _notify(self, **kwargs):
        # 实现通知逻辑
        pass
```

## 文件参考

- **基础提供者类**：`keep/providers/base/base_provider.py`
- **提供者模型**：`keep/providers/models/`
- **提供者工厂**：`keep/providers/providers_factory.py`
- **提供者异常**：`keep/exceptions/provider_exception.py`
- **示例提供者**：
  - 简单：`keep/providers/slack_provider/slack_provider.py`
  - 复杂：`keep/providers/datadog_provider/datadog_provider.py`
  - 数据库：`keep/providers/clickhouse_provider/clickhouse_provider.py`
  - 事件：`keep/providers/pagerduty_provider/pagerduty_provider.py`
  - 拓扑：`keep/providers/datadog_provider/datadog_provider.py`
- **测试**：`tests/test_*_provider.py`
- **文档**：`docs/providers/documentation/`
- **附加文档**： 
  - `docs/providers/adding-a-new-provider.mdx`
  - `docs/providers/provider-methods.mdx`
  - `docs/providers/linked-providers.mdx`

## 检查清单

- [ ] 创建提供者目录和文件
- [ ] 实现具有正确元数据的 AuthConfig 类
- [ ] 实现具有所需方法的提供者类
- [ ] 将提供者添加到 `__init__.py`
- [ ] 设置适当的 PROVIDER_DISPLAY_NAME、PROVIDER_CATEGORY 和 PROVIDER_TAGS
- [ ] 实现 `validate_config()` 和 `dispose()`
- [ ] 添加至少一个能力（`_notify`、`_query` 或 `_get_alerts`）
- [ ] 在 `docs/providers/documentation/` 中创建文档
- [ ] 编写单元测试
- [ ] 使用提供者工厂进行测试
- [ ] 优雅地处理错误
- [ ] 添加日志记录语句
- [ ] 在 Keep UI 中验证
- [ ] 如果支持 webhook，实现 `_format_alert()` 静态方法
- [ ] 如果支持 OAuth 2.0，将 OAUTH2_URL 设置为类属性
- [ ] 考虑实现 `validate_scopes()` 进行权限范围验证
- [ ] 考虑实现 `get_provider_metadata()` 进行提供者版本管理

## 获取帮助

- 查阅现有提供者以获取示例
- 查看基础提供者类以了解可用方法
- 查看测试文件以了解测试模式
- 在 Keep 的 GitHub 讨论或 issues 中提问
- 查阅[提供者方法文档](/providers/provider-methods)以了解高级能力
- 了解[链接提供者与连接提供者](/providers/linked-providers)
